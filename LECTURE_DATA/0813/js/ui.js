(function(global, $, undefined){
	'use strict';

	// UI 적용 대상 참조
	var $accordion = $('.ui-accordion');

	// UI 초기 실행
	var $accordion_contents = $accordion.find('[class*="-content"]').hide();

	// console.log($accordion_contents);


})(window, window.jQuery);